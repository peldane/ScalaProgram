object guess
{
    def main(args : Array[String])
    {
        def guess1(a: Int=2): Int=
        {
            a*a;
            println(a);
            println("Enter number:"+b)
            val b=scala.oi.StdIn.readInt();
            if(a=b)
            {
                println("good..........");
            }
            else
            {
                println("Don't sucesss..........")
            }
        }
    }
}