
object TimeTable
    def main(args: Array[String])
    {
        println("Enter a number to display Timetable");
        var input = Scala.ot.stdIn.readInt()';
        var mun= 0;
        for(num <-1-to)
        {
            println(iutput+" * " +num+"="+ (input*num) );
        } 
    }

}