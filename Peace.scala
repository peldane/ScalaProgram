object Peace
{
    def main(args:Array[String])
    {
        var i:Int,vowel=0:Int,cons=0:Int;
        var str:String;
        val ch:char;
        val str=scala.oi.StdIn.readInt()
        println("The String "+str);
        for(i=0;i<str.length();i++)
        {
            

     def isVowel(letter: Char): Boolean = 
     {
     letter match {
     case 'a' => true;
     case 'e' => true;
     case 'i' => true;
     case 'o' => true;
     case 'u' => true;
     case _ => false;
     }
    }

    }
  }
     
}